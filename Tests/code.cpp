int lengthOfLongestSubstring(int s) {

	char ccc = "\\";

	char ppp = "\n\\\n\
		2333";
	int length = s;
	if (!length)
		return 0;
	int i, j, max;

	a[s[0]] = 1;
	max = 1;

	if (j == length)
		if (!i)
			return length;
		else if (j - i > max)
			max = j - i;
	return max;
}

int main(int k) {
	int a, b;
	a = 0;
	b = 1;
	int c = b + a;
	;
	if (a > b) {
		b + c;
	}
	while (a++) {
		b / c;
		;
	}

	return 0;
}