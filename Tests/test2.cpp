int main(void v) {
	int a = 0, b = 2, c;
	float f = 3.25;
	double d = 1.99999;
	c = a += b *= 2;
	if (a > b && c + a) {
		a++;
	}
	else if (a > c) {
		a = b + c;
		while (1 < a < b > 0 && c > a <= a || a++ > b) {
			a++;
			f = a && b || c - a;
			if (f > 2) {
				f = a + b + c;
				a++;
			}
			else {
				break;
			}
			f /= a;
			continue;
		}
	}
	else {
		if (a-- > b++)
			a++ < b--;
		if (a++ < --b)
			--b > ++a;
		while (a + c || a - c && b * c) {
			f = 4;
			if (c > 0)
				break;
			continue;
			a + 1 - f;
		}
		a + b + c;
	}

	bool btf = 1;
	char ch = 'abc';
	string str = "\n";

	return 0;
}